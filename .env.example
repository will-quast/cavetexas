# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Configuration (for local development)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_PUBLIC_URL=http://localhost:8000

# Database Configuration
POSTGRES_PASSWORD=your-postgres-password
JWT_SECRET=your-jwt-secret

# Note: For local development, these are the default values used by the local Supabase instance
# The anon key and service role key are hardcoded in the local Supabase instance 